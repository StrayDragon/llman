（自动生成占位文件）

此文件用于让 `artifacts/testing_config_home/arena/contests/sdd_apply_v1.toml` 能通过存在性校验。

实际评估建议使用 `just sdd-prompts-eval`（或 `bash scripts/sdd-prompts-eval.sh`）在临时目录生成 baseline/candidate prompts，再运行 `llman x arena gen/vote/report`。
