kind: llman.sdd.spec
name: "sdd-bdd-mode-compat"
purpose: "规范 sdd 工具链在 BDD-on（Git-native feature-as-spec）与 BDD-off（未启用）两种项目配置下的行为合约与兼容性。当 config.yaml 的 bdd 段存在与否时，validate / change attach|checkpoint|diff / archive / index 等子命令 MUST 表现出符合预期的差异，且 BDD-off 项目不得因 .feature 文件而失败。"
valid_scope[1]: "llmanspec/specs/sdd-bdd-mode-compat"
requirements[11]{req_id,title,statement}:
  r26,validate 的 check 语义（runner 开关）,"validate 的 --check/--no-check 行为 MUST 按 bdd 段是否存在切换（此时 bdd 段仅作为 runner 开关，不再影响流程）：含 bdd 段时默认执行 bdd.run_command（对 live 分支树中的真实 .feature），--no-check 跳过；不含 bdd 段时 --check 不执行 runner 且不视为错误（仅输出 INFO）。统一流程下无论 bdd 段有无，validate 都校验 spec.toon 的 requirements 与 Gherkin 解析（若 .feature 存在）。"
  r57,Git-native change binding（统一流程）,"change MUST 绑定非默认 Git 分支与 immutable base SHA（统一流程，不再按 bdd 段分叉）：`llman sdd change start <id>` 为推荐入口（自动建分支 + clean-tree 门禁 + 绑定，见 sdd-workflow r111）；`llman sdd change attach <id>` 为共存命令（手动绑已有分支，含 --force 重绑）。`checkpoint` MUST 要求干净工作树并跑门禁；`diff` MUST 只读展示/导出 base...HEAD。`llman sdd change new` MUST 能创建 proposal 草稿。`llman sdd change delta` MUST 在任何模式下失败并提示已移除（统一 Git-native，见 sdd-workflow r115）。默认分支上 start/attach/checkpoint/archive MUST 失败。MUST NOT 再提供 `sdd solidify` 子命令。`change checkpoint` 与 `change start` MUST 接受并忽略 `--no-interactive` flag（对齐 change 子命令 flag 矩阵，便于 skill 统一传参）。"
  r78,index rebuild 的 feature embed,"index rebuild MUST 在含 bdd 段的项目中把 .feature 派生的 spec-level scenario（req_id 为空）编入 tree.json；不含 bdd 段的项目（无 .feature 或 .feature 仅作文档）不得产生 spec-level scenario。"
  r83,"无 bdd 段时 feature 仅作文档","validate 在无 bdd 段的项目中 MUST 静默忽略 .feature 文件作为可执行 harness（既不解析 Gherkin 也不因格式问题报错），但仍可将其中的 scenario 作为文档引用。统一流程下无 bdd 段不等于'无 specs'——specs 来源于 live spec.toon（在 feature 分支编辑）。"
  r5,Partitioned SSOT 分区权威（统一流程）,"统一 Git-native 流程下 MUST 采用 Partitioned SSOT（不再按 bdd 段开关分叉）：spec.toon 权威承载 requirements 与不可执行 scenarios；当 config 含 bdd 段时 *.feature 权威承载可执行场景 GWT，可执行场景 MUST 使用 @req:<req_id> 标签挂回 requirement；同一 scenario id 的完整 GWT MUST NOT 同时存在于 toon 与 feature。无论 bdd 段有无，可执行变更都直接编辑分支上的 live .feature（无 bdd 段时 .feature 仅作 Gherkin 文档，不被 runner 执行）。"
  r6,validate 链接与双写及全局唯一门禁,"BDD-on 时 validate MUST 校验：feature 中每个 @req:X 对应 toon requirement X 存在；同一 scenario id 可执行 GWT 双写在 --strict 下 MUST 为失败，且错误消息 MUST 列出每个冲突对的 (req_id, scenario.id)（例如 `dual-write: N executable scenario(s) ... : [(r12, login-ok), (r15, logout)]`），让 agent 无需二次排查即可定位；不可执行 toon scenario id MUST NOT 出现在 .feature；跨不同 main spec 的重复 req_id MUST 立即失败并附修复建议（见全局唯一合约）。--check 仍按 bdd.run_command 执行 harness。"
  r7,archive docs rename + ff-merge（统一流程）,"`llman sdd change archive` 统一行为（不再按 bdd 段分叉）：MUST 仅移动 change 文档到 changes/archive/YYYY-MM-DD-<id>/，随后自动 ff-merge feature 分支回分叉点分支（详见 sdd-workflow r113）。MUST NOT merge TOON delta（已废除 change/specs 路径）、MUST NOT apply feature_delta。活跃 `*.feature.delta.toon` MUST 作为迁移阻断（ERROR，提示跑 project migrate --kind partitioned 清理）。顶层 `sdd archive run` 为兼容别名但 MUST 走统一的 ff-merge 路径。"
  r85,"partition-migrate（重定义为 legacy→unified 清理）","llman sdd project migrate --kind partitioned MUST 把遗留的 BDD-off change/specs/ delta 与活跃 *.feature.delta.toon 清理为统一 Git-native 流程：移除 change/specs/ 目录、把双写的可执行 GWT 拆为 toon requirements + .feature @req；--dry-run MUST 只打印计划。统一流程已落地后的新项目不需要此命令（不是日常路径）。隐藏别名 partition-migrate 仍可用。"
  r86,全局 req_id 唯一性,"在 llmanspec/specs 主库中，每个 requirement 的 req_id MUST 在全部 capability 间全局唯一。通用 validate 路径（validate --all、validate <spec>、validate <change> 且会加载主库约束时）MUST 立即检测跨 capability 重复 req_id：默认与 --strict 下 MUST 判为失败并拦截；错误信息 MUST 指出冲突的 req_id 与涉及 capability，并给出可操作修复建议（例如改用 llman sdd spec next-req-id 分配新短 id，或 llman sdd spec resolve-req 查看归属）。非交互场景 MUST NOT 静默放过重复以免积累债务。"
  r91,批量 validate check 去重,"BDD-on 且 check mode 开启时：单 spec validate MUST 仍对该 capability 展开 bdd.run_command 占位符后执行一次；validate --all / validate --specs 及任何多 spec 批处理 MUST 按展开后的命令字符串去重，相同展开命令在该次 validate 进程内 MUST 至多实际执行一次，并将通过/失败结果复用到后续命中同一命令的 spec（失败 MUST 仍使相关校验失败）。占位符展开结果不同的命令 MUST 继续按 spec 分别执行。"
  r94,"finalize 单 commit 收尾（统一流程）","`llman sdd change finalize <id>` MUST 在单进程内执行（统一流程，不再仅限 BDD-on）：门禁（已 start/attach、当前分支 == binding.branch、非默认分支、无遗留 *.feature.delta.toon）→ validate 门禁（live strict + change stage，除非 --no-check）→ 写 frontmatter（checkpointed=true、checkpoint_sha=base_sha）→ docs-only archive rename → 自动 ff-merge（详见 sdd-workflow r113）。finalize MUST NOT 检查工作区 clean tree（使实现 diff + frontmatter + archive rename + ff-merge 可由调用方一次 git commit 收尾；ff-merge 失败时降级为提示且不回滚 rename）。finalize 写入的 checkpoint_sha MUST 等于 start/attach 时的 base_sha。finalize MUST 在任何写入前退出非零且不改 frontmatter / 不移动文件（gate 失败或 validate 失败时）。finalize MUST 幂等：重试时若 frontmatter 已含 checkpointed=true 且 checkpoint_sha 非空，MUST 跳过写入直接尝试 archive rename + ff-merge。finalize MUST 接受并忽略 --no-interactive。旧路径 checkpoint + archive（含双重 clean-tree 门禁与严格 sha 语义）MUST 保持不变作 fallback。"
scenarios[8]{req_id,id,given,when,then,feature}:
  r57,"git-binding-unified-note",约束层说明,"change start/attach（统一流程）","绑定分支与门禁；change delta 已移除；无 solidify；可执行见 git-binding feature",false
  r5,"partitioned-ssot-unified-note",约束层说明,"统一 Partitioned SSOT",toon 约束 + .feature harness；双写禁止；可执行场景见 .feature,false
  r6,"req-link-global-note",约束层说明,validate 检查 @req 与全局唯一,链接与跨 spec 撞车均按合约失败,false
  r7,"archive-ff-merge-unified-note",约束层说明,"统一 archive","docs rename + 自动 ff-merge；拒绝 legacy feature_delta；可执行见 archive-freeze-and-gates feature",false
  r85,"partition-migrate-legacy-cleanup-note",约束层说明,"partition-migrate --dry-run（legacy 清理）","清理 change/specs delta 与 feature.delta.toon；非日常路径；harness 场景见 .feature",false
  r86,"global-req-id-unique-note",约束层说明,两个 main spec 使用相同 req_id,validate 立即失败并含修复建议；可执行场景见 .feature,false
  r91,"batch-check-dedupe-note",约束层说明,无占位符 run_command 下 validate --specs,harness 场景见 .feature,false
  r94,"finalize-unified-note",约束层说明,"统一 finalize（已 start/attach 的实现收尾）","docs rename + 自动 ff-merge；调用 finalize 后工作区仅脏一次；可执行见 git-binding feature",false
