kind: llman.sdd.spec
name: "claude-code-runner"
purpose: 规范 `llman x cc`（Claude Code runner）的交互参数解析、引号处理与执行行为合约。
valid_scope[1]: "llmanspec/specs/claude-code-runner"
requirements[1]{req_id,title,statement}:
  r1,"claude-code-runner",规范 `llman x cc`（Claude Code runner）的交互参数解析、引号处理与执行行为合约。 The system MUST satisfy the scenarios in this spec.
scenarios[7]{req_id,id,given,when,then,feature}:
  r1,引号参数被保留,"用户在交互 args 提示中输入 {quoted_input}",解析参数,解析后的参数向量包含各自独立的逻辑参数,true
  r1,未闭合引号被拒绝,用户在交互 args 提示中输入含未闭合引号的字符串,解析参数,命令提示解析失败并拒绝执行,true
  r1,透传单个 flag,"用户运行 llman x cc -- --version",llman 执行 claude,"claude 命令参数包含 --version",true
  r1,透传多参数并保持顺序,"用户运行 llman x claude-code -- --message \"hello world\" --flag",llman 执行 claude,"claude 命令参数按顺序包含 --message、hello world 与 --flag",true
  r1,大写配置 pattern 也能匹配,"配置中含危险 pattern {upper_pattern}\n工具检查 {mixed_check}",安全检测执行,该 pattern 被命中并输出安全警告,true
  r1,注入 PATH 危险键被拒绝,"所选配置组含 PATH=/tmp/evil:$PATH","用户运行 llman x cc -- --version","命令失败并报告危险环境变量被拒绝\n未启动 claude",true
  r1,安全告警命中时中止执行,SecurityChecker 对当前 Claude settings 产生至少一条告警,"用户运行 llman x cc -- --version","stderr 含安全告警\n命令非零退出\n未启动 claude",true
