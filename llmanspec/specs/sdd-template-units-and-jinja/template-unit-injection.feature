# language: zh-CN
# 对应 spec: sdd-template-units-and-jinja — SDD 提示词组合 MUST 把可复用片段拆成独立的
# 模板单元文件（显式单元标识符 + 按 locale 的确定性查找）；渲染 MUST 基于 MiniJinja 注入，
# 且在缺失单元引用或必需变量时快速失败。
功能: 模板单元独立可发现并经 MiniJinja 注入渲染
  @req:r2
  场景: 维护者只改单个 unit 文件即可影响多个模板
    假如 维护者更新一个被多个 SDD 模板复用的提示单元
    当 改动落盘
    那么 改动仅在单个 unit 文件中完成
    而且 而且无需编辑无关模板

  @req:r2
  场景: locale 单元解析遵循确定性回退链
    假如 渲染器为 zh-Hans 解析某单元并回退到 en
    当 解析完成
    那么 按文档化的确定性回退链返回恰好一个解析出的单元源

  @req:r2
  场景: 缺失单元引用时渲染失败
    假如 模板引用了不存在的单元标识符
    当 执行渲染
    那么 渲染以非零退出并给出明确的缺失单元错误

  @req:r2
  场景: 缺失必需渲染变量时渲染失败
    假如 模板需要一个未提供的渲染变量
    当 执行渲染
    那么 渲染以非零退出并指明缺失的变量
