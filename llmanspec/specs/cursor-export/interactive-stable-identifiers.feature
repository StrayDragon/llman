# language: zh-CN
# 对应 spec: cursor-export — 交互式 export MUST 映射到稳定对话标识符（composer_id / tab_id），
# 导出与被选标识符集合一致；搜索结果 MUST 共享同一标识符空间；MUST 避免为未选中对话加载完整内容。
功能: 交互式稳定标识符与按需加载
  @req:r1
  场景: 排序展示不影响导出正确性
    假如 交互 UI 展示的是排序后的对话列表
    当 用户选择了 A 与 B
    而且 那么即使底层存储顺序不同，导出也只包含 A 与 B

  @req:r1
  场景: 搜索选择导出正确
    假如 用户通过 search more 流程选择了若干对话
    当 执行导出
    而且 那么导出结果与搜索选择完全一致

  @req:r1
  场景: 大库仅选少量对话时只加载选中项
    假如 workspace 中存在大量对话但用户只选择少量导出
    当 执行导出
    而且 那么仅为被选中对话加载完整内容
