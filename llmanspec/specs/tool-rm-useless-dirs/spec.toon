kind: llman.sdd.spec
name: "tool-rm-useless-dirs"
purpose: "规范 `llman tool rm-useless-dirs` 的命令别名与无用目录清理行为。"
valid_scope[1]: "llmanspec/specs/tool-rm-useless-dirs"
requirements[2]{req_id,title,statement}:
  r37,命令别名与整棵树生效的 protected 目录,"对应 spec: tool-rm-useless-dirs — CLI MUST 暴露 rm-useless-dirs 主命令，rm-empty-dirs 为 废弃别名（触发同行为并告警）；protected 目录 basenames MUST 在整棵扫描树生效（不删、不遍历）。"
  r70,useless 列表移除、列表可配置、legacy 键拒绝与 gitignore 解析,"对应 spec: tool-rm-useless-dirs — useless allowlist 目录即使非空也 MUST 移除；protected/useless 列表可经 tools.rm-useless-dirs 配置（mode: extend/override）；legacy 配置键 MUST 被拒绝； 默认 gitignore MUST 基于扫描目标所属仓库/目标自身解析（而非调用者 CWD）。"
scenarios[0]:
