kind: llman.sdd.spec
name: "config-schemas"
purpose: 规范 llman 配置 JSON schema 的生成与校验行为。
valid_scope[1]: "llmanspec/specs/config-schemas"
requirements[1]{req_id,title,statement}:
  r1,"config-schemas",规范 llman 配置 JSON schema 的生成与校验行为。 The system MUST satisfy the scenarios in this spec.
scenarios[13]{req_id,id,given,when,then,feature}:
  r1,在子目录运行 schema apply 定位到 repo 根,用户在 repo 的嵌套子目录中运行 llman self schema apply,命令执行,"schema header 被应用到 repo_root/.llman/config.yaml 与 repo_root/llmanspec/config.yaml（当文件存在时）\n不写入子目录下的同名路径",true
  r1,多条 header 行被重写为一条,"某 YAML 文件顶部包含多条 yaml-language-server schema 行",工具执行,"重写为顶部一条正确 header\n保留其余内容不变",true
  r1,生成 schema 文件,用户运行 llman self schema generate,生成完成,"全局/项目/llmanspec/sdd-eval playbook schema 被写入或刷新到指定路径",true
  r1,头注释缺失时写入,用户运行 llman self schema apply 且配置文件缺少 schema 头注释,命令执行,"写入对应的 yaml-language-server 头注释",true
  r1,头注释不匹配时修复,用户运行 llman self schema apply 且 schema URL 与目标不一致,命令执行,修复为正确的 schema URL,true
  r1,项目配置不含全局专用字段,"llman-project-config.schema.json 被生成",检查 schema,不包含 skills.dir,true
  r1,首次运行生成样例配置,CLI 启动且全局 config.yaml 不存在,CLI 运行,自动生成样例配置并写入 schema 头注释,true
  r1,已存在配置不被覆盖,CLI 启动且全局 config.yaml 已存在,CLI 运行,该文件保持不变,true
  r1,schema 校验失败返回非零,llman self schema check 发现 schema 无效或样例实例不匹配,命令运行,返回非零退出码并报告错误,true
  r1,使用真实全局配置作为样例,全局 config.yaml 存在且用户运行 llman self schema check,校验执行,使用该文件内容作为样例并对照 schema 校验,true
  r1,真实配置不可读或不可解析会失败,全局 config.yaml 存在但无法读取或无法解析为有效 YAML,运行 llman self schema check,"返回非零错误\n不回退到默认实例作为样例",true
  r1,全局配置不符合 schema 时非零,"CLI 读取全局 config.yaml 且内容与 llman-config.schema.json 不一致",CLI 运行,返回非零退出码并报告错误,true
  r1,llmanspec 配置不符合 schema 时非零,"llmanspec/config.yaml 与 llmanspec-config.schema.json 不一致",命令运行,返回非零退出码并报告错误,true
