kind: llman.sdd.spec
name: "sdd-ab-evaluation"
purpose: 规范可复现的 SDD prompts 与 agentic 工作流评测套件，含多风格对比与硬门禁校验。
valid_scope[1]: "llmanspec/specs/sdd-ab-evaluation"
requirements[1]{req_id,title,statement}:
  r1,"sdd-ab-evaluation",规范可复现的 SDD prompts 与 agentic 工作流评测套件，含多风格对比与硬门禁校验。 The system MUST satisfy the scenarios in this spec.
scenarios[13]{req_id,id,given,when,then,feature}:
  r1,报告优先安全与质量指标,评测报告生成,检查报告,"含质量与安全评分\n排在 token/latency 指标之前",true
  r1,agent 读编辑主 spec 文件,"multi-style agentic 评测运行",agent 执行,读取当前 workspace 的主 spec 文件,true
  r1,agent 读编辑 delta spec 文件,评测含某 change,agent 执行,读取该 change 的 delta spec 文件,true
  r1,同一任务在三种风格下跑通,"维护者执行 multi-style 评测",评测运行,为三种风格创建隔离 workspace 并分别运行相同任务,true
  r1,runner 评测前预置 baseline,一次新评测 run 开始,runner 准备,创建隔离 workspace 并预置语义等价的 baseline specs/changes,true
  r1,评测产物含 meta 快照,一次评测运行结束,检查临时根目录,存在 meta/（或等价）保存快照与日志,true
  r1,"--runs 产聚合报告","维护者运行 just sdd-claude-style-eval --runs 10（或等价）",评测完成,runner 写出聚合汇总到 meta/aggregate.md（和/或 .json）,true
  r1,同时启用硬门禁与可选 rubric,维护者启用 rubric judge 或 human judge,评测运行,"仍以硬门禁为基础\n同时输出 rubric/human 评分",true
  r1,docker 阿里云镜像构建并运行评测,维护者用传入的 build args（apt/npm/pypi mirror）构建 docker 镜像并运行评测,评测运行,可在容器内完成,true
  r1,在共享场景集上运行对比评测,用户对目标场景集执行评测流程,评测运行,系统在等价输入上运行 legacy 与新风格生成/评测,true
  r1,评测在隔离配置目录下运行,维护者运行评测脚本并从 agentdev/promptfoo/ 读取 fixtures 与模型列表,评测运行,Promptfoo 数据写入该流程创建的临时工作目录,true
  r1,评测套件位于 agentdev,维护者在仓库查找 promptfoo 评测套件位置,查找,评测套件位于 agentdev/promptfoo/,true
  r1,运行一次 Claude Code agentic 评测,维护者运行 Promptfoo fixture 驱动 Claude Code agent 完成一轮 SDD authoring + validate,评测运行,输出含 results.json 与 results.html,true
