kind: llman.sdd.spec
name: "sdd-context"
purpose: 规范 `llman sdd context` 命令的后端选项、默认值与 pageindex 检索行为。
valid_scope[1]: "llmanspec/specs/sdd-context"
requirements[3]{req_id,title,statement}:
  r1,"sdd-context",规范 `llman sdd context` 命令的后端选项、默认值与 pageindex 检索行为。 The system MUST satisfy the scenarios in this spec.
  r2,"Scenario-Aware Retrieval Partitioned",build_docs 与检索工具 MUST 按 Partitioned SSOT 暴露场景：可执行 GWT 以 *.feature 为权威；spec.toon 仅提供 requirements 与不可执行 scenarios。compute_spec_hash MUST 仍哈希 *.feature 与 spec.toon。get_document_structure MUST 能列出 requirement 下经由 @req 关联的 harness 场景 id。get_spec_content MUST 返回对应 given/when/then 全文且同一 scenario id MUST NOT 出现两份正文。旧 tree.json 无 scenarios 字段 MUST 仍可加载；无 .feature 的 spec MUST 行为与仅 toon 一致。
  r3,Feature Embedding 单次且 feature 优先,"index_rebuild 在 BDD-on 时 MUST 解析 *.feature 并编入索引；与 toon 可执行行同 id 碰撞时 MUST 以 feature 为准且 MUST NOT 再 embed toon 副本。toon 中不可执行 scenarios MUST 仍编入。req_id 优先取自 @req 标签；无标签时可为 spec-level 空 req_id 并在 validate 中按门禁告警。畸形 .feature MUST 跳过并警告而非中止 rebuild。BDD-off 或无 .feature 时输出 MUST 与仅 toon 一致。"
scenarios[3]{req_id,id,given,when,then,feature}:
  r2,"get-spec-content-no-dual-gwt",tree.json 中同 id 不应双份,调用 get_spec_content,返回的 given/when/then 每个 scenario id 至多一份,false
  r3,"rebuild-feature-wins-collision",spec 目录曾双写同 id GWT,执行 sdd index rebuild,索引中该 id 仅一份且正文来自 feature,false
  r3,"rebuild-embeds-feature-once",某 spec 含 .feature 与仅约束 toon,执行 sdd index rebuild,harness 场景来自 feature 且无 toon 可执行副本,false
