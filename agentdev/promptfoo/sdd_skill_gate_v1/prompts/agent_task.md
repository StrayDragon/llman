# NOTE: This file is OVERWRITTEN by the runner before promptfoo eval.
# The runner renders the target SKILL.md (stripped of frontmatter) into a
# system-prompt block, then appends the task_prompt below. This placeholder
# exists only so the fixture is structurally complete before the runner runs.

{{ task_prompt }}
