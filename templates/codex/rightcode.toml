# RightCode Codex Configuration
# Configuration for RightCode's codex models

model = "gpt-5.1-codex"
model_provider = "right"
approval_policy = "on-request"
sandbox_mode = "workspace-write"

[model_providers.right]
name = "RightCode"
base_url = "https://right.codes/codex/v1"
env_key = "OPENAI_API_KEY"
wire_api = "chat"

# Optional: Enable helpful features
[features]
view_image_tool = true
web_search_request = false

# Optional: Shell environment policy
[shell_environment_policy]
inherit = "core"
include_only = ["PATH", "HOME", "LANG"]

# Optional: Sandbox workspace-write settings
[sandbox_workspace_write]
exclude_tmpdir_env_var = false
exclude_slash_tmp = false
network_access = true
